# Backend de Finanzapp

El backend principal se encuentra en la carpeta `aws-api`.

## Estructura

- `aws-api/`: API principal (Node.js + Express + AWS + Supabase).

Por favor dirígete a [aws-api/README.md](./aws-api/README.md) para instrucciones de instalación y configuración.
